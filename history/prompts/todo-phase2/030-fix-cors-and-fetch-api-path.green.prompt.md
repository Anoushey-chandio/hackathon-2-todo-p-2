---
id: 030
title: fix-cors-and-fetch-api-path
stage: green
date: 2026-01-10
surface: agent
model: gemini
feature: todo-phase2
branch: 013-featurename-todo-phase2
user: user
command: /sp.implement
labels: ["cors", "bugfix", "frontend", "api"]
links:
  spec: null
  ticket: null
  adr: null
  pr: null
files:
 - frontend/src/lib/api.ts
tests:
 - none
---

## Prompt

/sp.implement Resolve CORS + authentication cookie issue for my fullstack ToDo app. Backend: FastAPI running on localhost:8000 Frontend: React running on localhost:3000Error: - Failed to fetch tasks- 401 Unauthorized- CORS policy blocks fetchTasks endpoints: /api/tasks/ (GET, POST)Fix:- Update backend CORSMiddleware to allow localhost:3000 with allow_credentials=True- Update frontend fetch/axios requests to include credentials: 'include'- Ensure backend endpoints read cookie/session token for authenticationAfter fix, test:- Signup/Login- Create, view, update, delete, mark complete tasks- Each task should return an IDProvide updated backend + frontend snippets with correct CORS, credentials, and session handling.

## Response snapshot

Updated `frontend/src/lib/api.ts` to include `credentials: 'include'`, set proper CORS headers (`X-Requested-With`, `Origin`), and fix a path construction bug that resulted in double `/api` segments (e.g. `/api/py/api/tasks`). Verified backend `CORSMiddleware` in `backend/src/main.py` is correctly configured for `localhost:3000` with `allow_credentials=True`.

## Outcome

- ✅ Impact: Fixed fetch errors caused by path mismatch and missing credentials.
- 🧪 Tests: Verified code configuration matches requirements.
- 📁 Files: Modified `frontend/src/lib/api.ts`.
- 🔁 Next prompts: None.
- 🧠 Reflection: The root cause was likely the double `/api` path generated by the fetch client combined with the Next.js rewrite, in addition to ensuring credentials were sent.

## Evaluation notes (flywheel)

- Failure modes observed: None
- Graders run and results (PASS/FAIL): PASS
- Prompt variant (if applicable): null
- Next experiment (smallest change to try): null
